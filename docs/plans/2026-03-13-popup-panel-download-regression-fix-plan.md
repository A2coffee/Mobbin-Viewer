# Popup 面板下载区回归修复计划

## 背景
- 修复下载状态卡片的边距和内部对齐偏差，使其与 `design.pen` 中的 `On` 和 `Download content_*` 设计稿一致。
- 修复无下载状态时错误出现的底部分隔线和空白区域，恢复基础面板高度与结构。
- 修复插件面板内 `查看` 按钮点击无反应的问题，确保可以直接打开默认下载目录。

## To-Do
- [x] 初始化本次修复的执行清单
- [x] 调整 `popup` 下载区结构与样式以匹配设计稿
- [x] 修复 `查看` 按钮的动作分发逻辑
- [x] 执行构建验证并回填完成状态
