# Mobbin Viewer 全屏关闭按钮偏移修复计划

## 背景
- 用户反馈：点击图片的 `fullscreen view (全屏查看)` 按钮后，进入 `Lightbox (灯箱)` 全屏界面时，右上角退出 `X icon (关闭图标)` 位置发生偏移。
- 本次目标：定位关闭按钮偏移来源，修复 `Lightbox (灯箱)` 关闭按钮样式，并通过构建验证产物。

## To-Do
- [x] 定位 `Lightbox (灯箱)` 关闭按钮的实现与样式来源
- [x] 修复退出 `X icon (关闭图标)` 的偏移问题
- [x] 执行构建验证并同步完成状态
