# Mobbin Viewer 0.0.4 状态图标资源接入记录

## 背景
- 用户要求停止继续手写状态 `SVG（矢量图标）`，改用 `C:\VibeCoding\Mobbin\icon` 目录中的图标资源。
- 本次仅替换状态类图标：插件开/关、滚动加载、下载中/重试中、打包中、成功、失败、网络异常。

## To-Do
- [x] 将 `icon` 目录中的状态图标复制进 `0.0.4` 工程
- [x] 改造下载浮窗，使用导入的 SVG 原始内容作为状态图标
- [x] 改造 `popup（弹出页）`，使用导入的开/关状态图标
- [x] 运行构建验证资源接入结果
